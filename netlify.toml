[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — every unknown path serves index.html so React Router takes over.
[[redirects]]
  from   = "/*"
  to    = "/index.html"
  status = 200
